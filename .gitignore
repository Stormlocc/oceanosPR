# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
/.venv

# Cachés de herramientas
/.mypy_cache/
/.pytest_cache/
/.ruff_cache/

# Skills locales de Codex — nunca se versionan (regla del PLAN)
/.agents/

# Tiers de ejecución: cada etapa los crea sola. El layout está en
# docs/topics/oceanos-pr-mvp/design/topology.md §2.1, no en placeholders.
/data/
/.work/

# Catálogo STAC local: estado generado por `scenes search`, no se versiona.
# Anclado a la raíz: sin la barra inicial también ignoraría src/oceanos/catalog/.
/catalog/

# Andamiaje de sesión de agentes
/.claude/worktrees/
/.superpowers/

# Cuarentena de limpieza (borrable tras validar)
/.quarantine-*/
